<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!-- Application theme for Launch Screen (Android 12+ API compatible) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Background color of the splash screen (avoids white flash) -->
        <item name="windowSplashScreenBackground">#1e1b4b</item>
        
        <!-- Icon shown in the center (OS managed) -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        
        <!-- Post-splash theme -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        
        <!-- Fallback for legacy (set window background to the generated splash image) -->
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>